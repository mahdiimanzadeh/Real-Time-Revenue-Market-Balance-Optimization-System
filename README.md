🛠️ خلاصه برنامه توسعه سیستم قیمت‌گذاری پویا

این پروژه در چهار فاز اصلی تقسیم‌بندی شده است که از تعریف مسئله و مهندسی داده شروع شده و تا استقرار، عملیاتی‌سازی و نظارت ادامه می‌یابد. هدف نهایی، ساخت یک سیستم خودکار برای پیش‌بینی تقاضا و عرضه و سپس تصمیم‌گیری بهینه در مورد قیمت‌گذاری برای حداکثرسازی درآمد و بهبود سطح خدمات (مثل ETA) است.

### فاز ۱: تعریف مسئله و مهندسی داده (Setup & Data Engineering)
این فاز بر آماده‌سازی زیرساخت داده و ویژگی‌ها متمرکز است.

#### هدف اصلی:
تعریف دقیق معیارهای عملکرد و ساخت پایپ‌لاین داده برای تولید ویژگی‌های غنی فضایی-زمانی.

#### خروجی کلیدی:
Feature Store کامل و آماده برای مدل‌سازی و DAGهای Airflow فعال برای خودکارسازی فرآیند داده.

#### زیرفازها:
1. **۱.۱. گرانولریتی و KPIها**
   - تعیین واحدهای مکانی (مانند Geohash سطح ۸) و زمانی (۵ دقیقه) برای پیش‌بینی و تعریف معیارهای موفقیت (مانند Gross Revenue، Service Level/ETA).

2. **۱.۲. معماری داده و ETL**
   - استخراج داده‌های تاریخی (سفر، کنسل شدن) و طراحی اسکیما برای ذخیره‌سازی در ClickHouse (برای داده‌های تحلیلی) و MySQL.

3. **۱.۳. ارکستراسیون (Airflow)**
   - ساخت DAGهای Airflow برای خودکارسازی استخراج، آماده‌سازی و مهندسی ویژگی‌ها به‌صورت روزانه/ساعتی.

4. **۱.۴. مهندسی ویژگی‌ها**
   - ساخت ویژگی‌های پیشرفته فضایی-زمانی (مانند تعداد رانندگان مجاور، روند تقاضای ۲۴ ساعته، وضعیت آب و هوا) برای مدل.

### فاز ۲: مدل‌سازی پیشرفته و بهینه‌سازی (Advanced Modeling & Optimization)
در این فاز، هسته هوشمند سیستم شامل مدل‌های پیش‌بینی و الگوریتم تصمیم‌گیری ساخته می‌شود.

#### هدف اصلی:
توسعه مدل‌های پیش‌بینی تقاضا/عرضه و الگوریتم بهینه‌سازی قیمت‌گذاری.

#### خروجی کلیدی:
مدل‌های آموزش‌دیده (Time Series/GNN) و ماژول کد تصمیم‌گیری (RL/Optimization) که ضریب قیمت‌گذاری بهینه را محاسبه می‌کند.

#### زیرفازها:
1. **۲.۱. مدل پیش‌بینی**
   - آموزش مدل‌های Time Series (LSTM) یا GNN برای پیش‌بینی دقیق تقاضا ($\hat{D}$) و عرضه ($\hat{S}$) برای ۵ دقیقه آینده در هر منطقه (Geohash).

2. **۲.۲. تابع هزینه/پاداش**
   - تعریف تابع پاداش چندهدفه که هدف آن حداکثرسازی Revenue و حداقل‌سازی ETA و کنسلی است.

3. **۲.۳. الگوریتم تصمیم‌گیری**
   - پیاده‌سازی الگوریتم Reinforcement Learning (RL) یا روش‌های بهینه‌سازی برای محاسبه ضریب قیمت‌گذاری بهینه ($S_{i, t}$) با استفاده از خروجی مدل‌های پیش‌بینی.

### فاز ۳: عملیاتی‌سازی و استقرار (Productionization & Deployment)
تمرکز این فاز بر تبدیل مدل‌ها به یک سرویس قابل استفاده در محیط عملیاتی (Production) است.

#### هدف اصلی:
بسته‌بندی مدل‌ها در قالب یک سرویس REST API و ساخت پایپ‌لاین به‌روزرسانی مدل.

#### خروجی کلیدی:
Docker Image آماده برای سرویس‌دهی مدل و DAGهای Airflow برای CI/CD (آموزش مجدد و استقرار مدل).

#### زیرفازها:
1. **۳.۱. کانتینرسازی سرویس**
   - بسته‌بندی مدل‌ها در یک سرویس REST API (مانند FastAPI) و ساخت Docker Image برای استقرار (Model Serving).

2. **۳.۲. پایپ‌لاین به‌روزرسانی**
   - ساخت DAGهای Airflow برای آموزش مجدد (Retraining) دوره‌ای مدل‌ها (مثلاً هفتگی) و به‌روزرسانی خودکار پارامترهای آن‌ها.

3. **۳.۳. A/B تست**
   - پیاده‌سازی منطق برای هدایت بخش کوچکی از ترافیک (مثلاً ۵٪) به مدل جدید جهت تست کنترل شده در محیط عملیاتی.

### فاز ۴: ارزیابی و نظارت (Evaluation & Monitoring)
این فاز شامل ارزیابی عملکرد مدل در دنیای واقعی و نظارت مستمر بر آن است.

#### هدف اصلی:
تحلیل نتایج A/B تست، اثبات ارزش افزوده سیستم و ایجاد داشبوردهای مانیتورینگ برای جلوگیری از افت عملکرد.

#### خروجی کلیدی:
گزارش آماری جامع از نتایج A/B تست و داشبوردهای فعال Real-Time با سیستم هشدار.

#### زیرفازها:
1. **۴.۱. تحلیل A/B تست**
   - انجام تحلیل آماری برای اثبات معنی‌دار بودن بهبود در KPIها (مانند افزایش درآمد (Lift in Revenue) و کاهش ETA).

2. **۴.۲. مانیتورینگ عملکرد**
   - طراحی داشبوردهای Real-Time برای رصد توزیع ضریب قیمت‌گذاری، سلامت مدل (Model Health) و میزان Drift آن نسبت به داده‌های واقعی.

3. **۴.۳. سیستم هشدار (Alerting)**
   - تعریف آستانه‌های هشدار برای KPIها (مانند عبور ETA از آستانه مشخص) و خطای پیش‌بینی مدل برای واکنش سریع به مشکلات.
